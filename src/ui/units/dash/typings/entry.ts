// TODO: take it from index, but first discuss with Kirill where we will take the types for Entry
import type {EntryIncludePermissions} from 'ui';

export interface DashEntry extends EntryIncludePermissions {}
