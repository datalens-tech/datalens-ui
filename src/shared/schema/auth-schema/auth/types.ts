export type SigninArgs = {
    login: string;
    password: string;
};

export type SignupArgs = {
    login: string;
    email: string;
    firstName: string;
    lastName: string;
    password: string;
};
