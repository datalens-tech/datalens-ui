import type {Palette} from '../types';
import {PALETTE_ID} from '../types';

export default {
    id: PALETTE_ID.GREEN_ORANGE_6,
    scheme: ['#ffd94a', '#ff7f0f', '#3cb7cc', '#39737c', '#32a251', '#b85a0d'],
} as Palette;
