import type {Palette} from '../types';
import {PALETTE_ID} from '../types';

export default {
    id: PALETTE_ID.GRAY_5,
    scheme: ['#cfcfcf', '#a5acaf', '#8f8782', '#60636a', '#414451'],
} as Palette;
