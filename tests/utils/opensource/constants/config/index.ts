import type {TestsParametrizationConfig} from '../../../../types/config';
import {wizard} from './wizard';
import {workbook} from './workbook';

export const config: TestsParametrizationConfig = {
    wizard,
    workbook,
};
