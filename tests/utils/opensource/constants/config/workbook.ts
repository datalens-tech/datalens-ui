import type {WorkbookParametrizationConfig} from '../../../../types/workbook';
import {DEFAULT_WORKBOOK_ID} from '../common';

export const workbook: WorkbookParametrizationConfig = {
    urls: {},
    ids: {Default: DEFAULT_WORKBOOK_ID},
};
