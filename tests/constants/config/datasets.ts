import {DatasetsParametrizationConfig} from '../../types/config/datasets';
import {DatasetsEntities} from '../test-entities/datasets';

export const datasets: DatasetsParametrizationConfig = {
    entities: DatasetsEntities,
};
